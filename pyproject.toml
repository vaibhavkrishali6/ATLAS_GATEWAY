[project]
name = "atlas"
version = "0.1.0"
description = "Atlas gateway and independently deployable services"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1.0",
    "pydantic-settings>=2.6,<3.0",
    "uvicorn[standard]>=0.30,<1.0",
]
